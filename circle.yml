# clone cdk too
checkout:
  post:
    - git submodule sync
    - git submodule update --init --recursive

# fix gradlew not begin marked as executable
dependencies:
  override:
    - echo y | android update sdk --no-ui --all --filter tools,platform-tools,android-24,build-tools-24.0.1
    - echo y | android update sdk --no-ui --all --filter extra-android-m2repository,extra-google-m2repository,extra-android-support
    - chmod +x ./gradlew
    - ./gradlew dependencies
