# fix gradlew not begin marked as executable
dependencies:
  override:
    - echo y | android update sdk --no-ui --all --filter tool,extra-android-m2repository,android-23,android-19,build-tools-23.0.3
    - chmod +x ./gradlew
    - ./gradlew dependencies
